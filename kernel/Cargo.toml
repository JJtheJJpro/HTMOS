[package]
name = "htmkrnl"
version = "0.0.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
htmos-boot-info = "0.9.0"
libm = { version = "0.2.15", default-features = false }
r-efi = "5.3.0"
widestring = { version = "1.2.1", default-features = false }
