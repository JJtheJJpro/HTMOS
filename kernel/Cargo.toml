[package]
name = "htmkrnl"
version = "0.0.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
htmos-boot-info = "0.9.0"
r-efi = "5.3.0"
widestring = { version = "1.2.1", default-features = false }
