[package]
name    = "bootloader-bios"
version = "0.1.0"
edition = "2021"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true

[profile.dev]
panic = "abort"

[dependencies]
htmos-boot-info = "0.9.1"
